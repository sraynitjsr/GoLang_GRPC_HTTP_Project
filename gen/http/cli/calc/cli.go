// Code generated by goa v3.11.1, DO NOT EDIT.
//
// calc HTTP client CLI support package
//
// Command:
// $ goa gen github.com/sraynitjsr/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	calcc "github.com/sraynitjsr/gen/http/calc/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `calc (add|sub|multiply|divide)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calc add --a 5401762099778430809 --b 1918630006328122782` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcAddFlags = flag.NewFlagSet("add", flag.ExitOnError)
		calcAddAFlag = calcAddFlags.String("a", "REQUIRED", "Left operand")
		calcAddBFlag = calcAddFlags.String("b", "REQUIRED", "Right operand")

		calcSubFlags = flag.NewFlagSet("sub", flag.ExitOnError)
		calcSubAFlag = calcSubFlags.String("a", "REQUIRED", "Left operand")
		calcSubBFlag = calcSubFlags.String("b", "REQUIRED", "Right operand")

		calcMultiplyFlags = flag.NewFlagSet("multiply", flag.ExitOnError)
		calcMultiplyAFlag = calcMultiplyFlags.String("a", "REQUIRED", "Left operand")
		calcMultiplyBFlag = calcMultiplyFlags.String("b", "REQUIRED", "Right operand")

		calcDivideFlags = flag.NewFlagSet("divide", flag.ExitOnError)
		calcDivideAFlag = calcDivideFlags.String("a", "REQUIRED", "Left operand")
		calcDivideBFlag = calcDivideFlags.String("b", "REQUIRED", "Right operand")
	)
	calcFlags.Usage = calcUsage
	calcAddFlags.Usage = calcAddUsage
	calcSubFlags.Usage = calcSubUsage
	calcMultiplyFlags.Usage = calcMultiplyUsage
	calcDivideFlags.Usage = calcDivideUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calc":
			svcf = calcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calc":
			switch epn {
			case "add":
				epf = calcAddFlags

			case "sub":
				epf = calcSubFlags

			case "multiply":
				epf = calcMultiplyFlags

			case "divide":
				epf = calcDivideFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calc":
			c := calcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calcc.BuildAddPayload(*calcAddAFlag, *calcAddBFlag)
			case "sub":
				endpoint = c.Sub()
				data, err = calcc.BuildSubPayload(*calcSubAFlag, *calcSubBFlag)
			case "multiply":
				endpoint = c.Multiply()
				data, err = calcc.BuildMultiplyPayload(*calcMultiplyAFlag, *calcMultiplyBFlag)
			case "divide":
				endpoint = c.Divide()
				data, err = calcc.BuildDividePayload(*calcDivideAFlag, *calcDivideBFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintf(os.Stderr, `The calc service performs operations on numbers.
Usage:
    %[1]s [globalflags] calc COMMAND [flags]

COMMAND:
    add: Add implements add.
    sub: Sub implements sub.
    multiply: Multiply implements multiply.
    divide: Divide implements divide.

Additional help:
    %[1]s calc COMMAND --help
`, os.Args[0])
}
func calcAddUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calc add -a INT -b INT

Add implements add.
    -a INT: Left operand
    -b INT: Right operand

Example:
    %[1]s calc add --a 5401762099778430809 --b 1918630006328122782
`, os.Args[0])
}

func calcSubUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calc sub -a INT -b INT

Sub implements sub.
    -a INT: Left operand
    -b INT: Right operand

Example:
    %[1]s calc sub --a 5855163322465186600 --b 1385266597691519195
`, os.Args[0])
}

func calcMultiplyUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calc multiply -a INT -b INT

Multiply implements multiply.
    -a INT: Left operand
    -b INT: Right operand

Example:
    %[1]s calc multiply --a 7309877832173772408 --b 3237209857320107068
`, os.Args[0])
}

func calcDivideUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calc divide -a INT -b INT

Divide implements divide.
    -a INT: Left operand
    -b INT: Right operand

Example:
    %[1]s calc divide --a 9092197569596372935 --b 2937026320033011362
`, os.Args[0])
}
